let firstNumber = 5;
let secondNumber = 9;

function compare(first, second) {
	if (first > second) {
		let highest = first;
		console.log(highest);
	}
	else {
		let highest = second;
		console.log(highest);
	}
}

compare(firstNumber, secondNumber);